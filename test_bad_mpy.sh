#!/bin/bash
DIR=`dirname $0`

rm -rf "$DIR"/CIRCUITPY-badmpy/
cp -r "$DIR"/board-v7 "$DIR"/CIRCUITPY-badmpy
cp -r "$DIR"/lib-bad-mpy "$DIR"/CIRCUITPY-badmpy/lib

#
echo -e "
####################################################################
####################################################################
"
circup --path "$DIR"/CIRCUITPY-badmpy list
echo -e "
####################################################################
####################################################################
"
circup --path "$DIR"/CIRCUITPY-badmpy install neopixel
echo -e "
####################################################################
####################################################################
"
circup --path "$DIR"/CIRCUITPY-badmpy update
echo -e "
####################################################################
####################################################################
"
circup --path "$DIR"/CIRCUITPY-badmpy list
#
